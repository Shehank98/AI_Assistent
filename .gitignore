# Python
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/
.venv/
venv/
env/

# Environment / secrets — NEVER commit these
.env
*.env

# OAuth credentials and tokens — NEVER commit these
client_secret.json
*_token.json
auth/*.json

# Jarvis local data (contains encrypted personal data)
# ~/.jarvis/ is outside the repo, but just in case
.jarvis/

# Editor
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db
