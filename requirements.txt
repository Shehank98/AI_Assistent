# JARVIS — Python requirements
# ─────────────────────────────────────────────────────────────────

# Core
google-genai>=1.0.0

# Web server
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
aiofiles>=23.2.0
python-multipart>=0.0.9
python-dotenv>=1.0.0

# Encryption at rest (memory store)
cryptography>=42.0.0

# PostgreSQL (Railway production; falls back to SQLite locally when DATABASE_URL unset)
psycopg2-binary>=2.9.9

# Background scheduler — proactive email/calendar/goal alerts
apscheduler>=3.10.0

# Google Cloud TTS client library
google-cloud-texttospeech>=2.16.0

# Google (Gmail + Calendar)
google-auth>=2.29.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.127.0

# Spotify
spotipy>=2.23.0

# News RSS fallback (optional — falls back to raw urllib if build fails)
# feedparser>=6.0.10

# Web fetch (readable text extraction)
trafilatura>=1.8.0

# YouTube search (no API key needed)
# youtube-search-python>=1.6.6   # install with: pip install youtube-search-python

# YouTube transcript extraction (no API key needed)
youtube-transcript-api>=0.6.2

# RAG — LangChain document loaders + format support
langchain-community>=0.2.0      # loaders: PDF, Word, HTML, JSON, text
langchain-core>=0.2.0           # Document class
langchain-text-splitters>=0.2.0 # RecursiveCharacterTextSplitter (moved here in 0.2+)
pandas>=2.0.0                # Excel + CSV (both .xls and .xlsx via engine dispatch)
xlrd>=2.0.1                  # .xls engine for pandas (old Excel format)
openpyxl>=3.1.0              # .xlsx engine for pandas
pypdf>=4.0.0                 # PDF via LangChain PyPDFLoader
docx2txt>=0.8                # Word (.docx) via LangChain Docx2txtLoader
python-pptx>=0.6.23          # PowerPoint (.pptx) — parsed directly

# WhatsApp (desktop only — requires WhatsApp Web in browser)
# pywhatkit>=5.4

# Voice — Speech to Text (install when ready for voice mode)
# openai-whisper
# pyaudio          # apt install portaudio19-dev && pip install pyaudio

# Voice — Text to Speech (cascade: first available wins)
# pyttsx3
# TTS              # Coqui: pip install TTS
# elevenlabs

# Wake word (optional)
# pvporcupine
